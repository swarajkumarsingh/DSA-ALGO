#include <iostream>

int main(){
    int n = 123;

    // Get last number of the digit
    int lastDigit = n % 10;

    // Remove the last number 
    int new_n = n / 10;

    // check if the number is in the range of integer
    if ((ans > INT_MAX / 10) || (ans < INT_MIN / 10)){
        // ... code 
    }

    return 0;
}
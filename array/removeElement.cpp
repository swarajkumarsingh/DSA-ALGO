#include <iostream>
#include <vector>

using namespace std;

vector<int> removeElement(vector<int> nums, int ele)
{
    vector<int> newNums = {};

    for (int i = 0; i < nums.size(); i++)
    {
        
    }
    

    return newNums;
}

int main()
{
    removeElement({1, 2}, 2);
    return 0;
}